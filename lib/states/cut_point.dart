import 'package:flutter/material.dart';


class CutPoint extends StatelessWidget {
  const CutPoint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}